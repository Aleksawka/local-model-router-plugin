# Port parser test fixture

Минимальный dependency-free проект для T09.

Исходный baseline:

```bash
node --test test/parse-port.test.js
```

Все исходные тесты должны проходить. В T09-C Junior разрешено добавить только test cases для значений `0` и `65536` в `test/parse-port.test.js`; `src/parse-port.js` менять запрещено.
